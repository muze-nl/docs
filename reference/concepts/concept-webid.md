# Concept: WebID

A WebID is a method by which an application can authenticate a user using a remote identity and authentication server. WebIDs are used by Solid aplications to authenticate users.

Physically, a WebID is a URL which returns authentication and identity data. An application queries it to identify a user, verify that they are authenticated, and grant permissions for the application itself and for the user’s pod.
