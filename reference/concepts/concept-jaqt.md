# Concept: JAQT

JAQT, pronounced “Jacket”, is a software library providing a simple query engine for Javascript arrays and objects.

Instead of implementing its own query language in a similar manner to for example SQL, it uses Javascript itself to form the queries.
